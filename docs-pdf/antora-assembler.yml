root_level: 0
component_versions: '**'
asciidoc:
  attributes:
    source-highlighter: rouge
build:
  command: bundle exec asciidoctor-pdf --trace --sourcemap
  keep_aggregate_source: true
  publish: true


# bundle exec asciidoctor-pdf --trace --sourcemap